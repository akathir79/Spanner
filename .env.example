# SPANNER Environment Variables
# Copy this file to .env and fill in your actual values

# Database Configuration (Required)
DATABASE_URL="postgresql://username:password@hostname:port/database"

# Optional API Keys (can be set via admin panel after setup)
GEMINI_API_KEY="your_google_gemini_api_key_here"
SMS_API_KEY="your_sms_service_api_key_here"

# Development Settings (auto-configured in Replit)
NODE_ENV="development"
PORT="5000"

# Note: For Replit deployment, only DATABASE_URL is required
# Other keys can be managed through the admin panel's Key Management system